// Token-mint server stub — see README. Becomes functional when the
// /v1/client_tokens endpoint ships (API program P5).
console.log("primate-vision-client token-mint stub — not yet functional (P5)");
